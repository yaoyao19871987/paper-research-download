# 今日修改记录（2026-03-21）

## 目标

把 CNKI 流程从“旧仓库思路参考”收敛到“当前页面直接抓”，并把浏览器统一到 Chrome 调试复用路线。

## 今日完成

### 1. CNKI 检索导航降频

文件：
[D:\Code\paper-download\Chinese paper search\cnkiLRspider\cnki_common.py](D:\Code\paper-download\Chinese paper search\cnkiLRspider\cnki_common.py)
[D:\Code\paper-download\Chinese paper search\cnkiLRspider\research_pipeline.py](D:\Code\paper-download\Chinese paper search\cnkiLRspider\research_pipeline.py)

完成内容：

- 新增 `human_pause()`，把延迟分成 `micro / normal / read / think / between_queries / after_nav`
- 新增 `navigate_to_search()`，后续查询不再重复走两次 `driver.get()`
- `_crawl_query()` 改成“同一查询提交一次，再切 `default / cited / download` 三个视图”
- `_crawl_queries()` 改成按查询索引传 `is_first_query`

### 2. 结果页抓取稳定性增强

文件：
[D:\Code\paper-download\Chinese paper search\cnkiLRspider\cnki_common.py](D:\Code\paper-download\Chinese paper search\cnkiLRspider\cnki_common.py)

完成内容：

- `extract_result_cards()` 增加 `stale element` 重试
- `_pick_text / _pick_attr / _join_text` 增强异常容错
- `dump_debug_html()` 在窗口丢失时不再二次报错

### 3. 当前页面摘要补抓链路

文件：
[D:\Code\paper-download\Chinese paper search\cnkiLRspider\cnki_common.py](D:\Code\paper-download\Chinese paper search\cnkiLRspider\cnki_common.py)
[D:\Code\paper-download\Chinese paper search\cnkiLRspider\research_pipeline.py](D:\Code\paper-download\Chinese paper search\cnkiLRspider\research_pipeline.py)

完成内容：

- 结果页继续抓题录和详情页链接
- `page_url` 规范化时保留 `v / cid / articleid / uniplatform / language` 等关键参数
- 新增 `extract_detail_metadata()`，从当前 `kcms2/article/abstract` 页提取：
  - 标题
  - 作者
  - 机构
  - 期刊
  - 年份
  - 摘要
- 新增 `fetch_detail_metadata()`，缺摘要时开新标签页补抓
- `_enrich_rows_with_details()` 接入研究流水线，支持每页限量补抓
- 详情页单篇失败时只记日志，不让整轮查询直接失败

### 4. 浏览器统一到 Chrome

文件：
[D:\Code\paper-download\Chinese paper search\cnkiLRspider\cnki_common.py](D:\Code\paper-download\Chinese paper search\cnkiLRspider\cnki_common.py)

完成内容：

- `build_driver()` 默认优先 `chrome`
- 支持 `CNKI_DEBUGGER_ADDRESS`
- 新增自动探测调试端口：
  - 默认扫描 `127.0.0.1:9222,9223,9333`
  - 命中后自动 attach

### 5. 调试复用辅助脚本

新增文件：
[D:\Code\paper-download\start-cnki-chrome-debug.cmd](D:\Code\paper-download\start-cnki-chrome-debug.cmd)
[D:\Code\paper-download\run-cnki-pipeline-debug.cmd](D:\Code\paper-download\run-cnki-pipeline-debug.cmd)
[D:\Code\paper-download\CHROME_DEBUG_使用说明.md](D:\Code\paper-download\CHROME_DEBUG_使用说明.md)

用途：

- 一键拉起带调试端口的 Chrome
- 一键用该 Chrome 会话跑 CNKI 研究流水线
- 降低后续人工操作成本

## 当前结论

- 旧 GitHub 仓库仍可参考字段和思路，但主流程已经不再依赖 `scratch.py / scratch2.py / scratch3.py`
- 当前 CNKI 页面可以直接抓题录
- 当前 CNKI 详情页可以直接抓摘要
- 剩余最大变量已经不是“能不能抓”，而是“复用同一 Chrome 会话后，验证触发频率和 renderer timeout 能不能进一步压低”

## 下一步

- 用调试 Chrome 复用同一浏览器会话，重新验证摘要补抓链路
- 复跑一轮 round1，确认 `papers_master.csv` 中摘要字段真实增多
- 再继续 round2 和候选下载清单联调
